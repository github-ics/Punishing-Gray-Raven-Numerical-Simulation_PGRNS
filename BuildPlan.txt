programing terms:
buff num should be a const int or #Define it.
use Vector instead of raw array
using using dynamic memory allocation (e.g., std::vector) instead of static arrays
arrange the comment in code file(may at the end of each version)
learn about  std:: prefix and decide use it or not

Usage terms:
collect memberdata,environment data, enimy data
input with excel
built an Android interface, webside, app or api for wechat ?
complete a introduction Doc depend on GPT 3.5

Building sequence:
buff num should be a const int or #Define it.

use Vector instead of raw array

input with excel

built an Android interface, webside, app or api for wechat 

complete a introduction Doc depend on GPT 3.5